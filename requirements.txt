# Pinterest Scraper Dependencies

# Core dependencies
playwright==1.49.0
aiohttp==3.11.11
python-dotenv==1.0.1

# Google Drive upload
google-api-python-client==2.154.0
google-auth-oauthlib==1.2.1
google-auth==2.37.0

# Optional: Image-based NSFW detection
# Choose ONE of the following backends:

# NudeNet (Recommended) - Lightweight, fast, no heavy dependencies
nudenet==3.4.2

# PyTorch backend (Alternative) - More accurate, requires PyTorch
# torch==2.1.0
# torchvision==0.16.0
# Pillow==11.1.0
